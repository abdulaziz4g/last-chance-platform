# TEMPLATE ONLY — real secrets come from the secret manager (External
# Secrets Operator / Vault / cloud KMS). Never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: lastchance-secrets
  namespace: lastchance
type: Opaque
stringData:
  DATABASE_URL: "postgres://USER:PASSWORD@managed-pg-host:5432/lastchance"
  REDIS_URL: "redis://managed-redis-host:6379"
  JWT_SECRET: "CHANGE_ME_32+_RANDOM_CHARS"
  STRIPE_SECRET_KEY: ""
  STRIPE_WEBHOOK_SECRET: ""
